import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thyroidmgmt.app',
  appName: 'Thyroid Nodule Management',
  webDir: 'dist'
};

export default config;
